%p#notice= notice
%h1 Contacts
%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Cellphone
      %th{:colspan => "3"}
  %tbody
    - @contacts.each do |contact|
      %tr
        %td= contact.fname
        %td= contact.lname
        %td= contact.cellphone
        %td= link_to 'Show', contact
        %td= link_to 'Edit', edit_contact_path(contact)
        %td= link_to 'Delete', contact, method: :delete, data: { confirm: 'Are you sure?' }
%h4 TODO: add "Sync with device" button and functionality
%br/
= link_to 'New Contact', new_contact_path
= link_to 'Back to profile', edit_user_registration_path
